name: dart_app
description: Full profesyonel dart istatistik uygulaması
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.20.0
  cloud_firestore: ^5.5.0
  sqflite: ^2.2.8+4
  path_provider: ^2.1.12
  provider: ^6.0.6
  charts_flutter: ^0.12.0
  flutter_local_notifications: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png